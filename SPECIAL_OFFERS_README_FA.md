# پیشنهاد ویژه زمان‌دار و فروش محدود

این نسخه دو قابلیت زیر را به ربات اضافه می‌کند:

- نمایش پیشنهاد آینده از زمان ساخت، همراه شمارش معکوس تا شروع
- فروش محدود با موجودی واقعی، شمارش معکوس و بسته‌شدن خودکار در پایان زمان یا اتمام موجودی

دکمه پیشنهاد همیشه به‌صورت یک دکمه مستقل در اولین ردیف منوی اصلی نمایش داده می‌شود. اگر زمان شروع هنوز نرسیده باشد، دکمه با قالب هیجانی `⏱ 01:12:15 تا پیشنهاد امروز 🔥` نمایش داده می‌شود. عنوان، پلن، حجم، مدت و درصد تخفیف تا لحظه شروع کاملاً مخفی هستند و لمس دکمه فقط تایمر همان منو را بروزرسانی می‌کند. خرید نیز تا رسیدن زمان شروع غیرفعال است.

پس از رسیدن شمارش معکوس به صفر، متن دکمه به `🔥 پیشنهاد امروز فعال شد! 🔥` تغییر می‌کند و از آن لحظه صفحه مشخصات و دکمه خرید نمایش داده می‌شوند.

در زمان انتظار، یک دکمه مستقل `🔔 موقع شروع خبرم کن` زیر ردیف تایمر دیده می‌شود. کاربر می‌تواند اعلان را فعال یا لغو کند. بیدارباش اختصاصی با PHP-CLI و اتصال مستقیم به دیتابیس در لحظه شروع پیام فعال‌شدن پیشنهاد را همراه دکمه مشاهده برای مشترکان می‌فرستد و کرون یک‌دقیقه‌ای نیز به‌عنوان مسیر پشتیبان فعال است؛ جزئیات پیشنهاد پیش از شروع همچنان مخفی می‌مانند.

فاکتور پیشنهاد ویژه قالب مستقلی دارد و فقط مبلغ نهایی تخفیف‌خورده‌ای را نمایش می‌دهد که در رکورد پرداخت ذخیره شده است. فاکتور حتی با موجودی ناکافی کیف پول صادر می‌شود و کمبود موجودی فقط هنگام فشردن دکمه پرداخت اعلام خواهد شد. پرداخت پیشنهاد فقط با موجودی کیف پول داخل ربات ممکن است؛ کارت‌به‌کارت، رمزارز و درگاه‌های آنلاین برای این فاکتور نمایش داده نمی‌شوند و لینک‌های قدیمی نیز پذیرفته نخواهند شد.

## ساخت پیشنهاد توسط مدیر

از مسیر `مدیریت ربات ⚙️` ← `🔥 پیشنهادها و فروش محدود` ← `➕ ساخت پیشنهاد جدید` وارد شوید و مراحل زیر را انجام دهید:

1. انتخاب پلن
2. واردکردن عنوان، مثل `50GB / 60 روزه`
3. واردکردن درصد تخفیف
4. تعیین زمان شروع
5. تعیین زمان پایان
6. تعیین تعداد قابل فروش
7. تعیین سقف تعداد خرید موفق برای هر کاربر؛ مثلاً `2` یعنی هر کاربر حداکثر دو بار می‌تواند پیشنهاد را بخرد

زمان را می‌توان با تاریخ شمسی مثل `1405/05/28 18:00` یا تاریخ میلادی مثل `2026-08-19 18:00` وارد کرد. برای شروع فوری، عبارت `الان` قابل استفاده است. منطقه زمانی محاسبات تهران است.

## منطق موجودی

موجودی با کلیک روی دکمه یا ایجاد فاکتور کم نمی‌شود. فقط پرداخت موفقی که واقعاً به ساخت سفارش در جدول سفارش‌ها منجر شود از موجودی کم می‌کند. پرداخت ناموفق یا لغوشده موجودی را تغییر نمی‌دهد.

سهمیه هر کاربر نیز دقیقاً با همین منطق محاسبه می‌شود. صفحه پیشنهاد «سقف خرید هر کاربر» و «سهمیه باقی‌مانده شما» را نشان می‌دهد. پس از مصرف کامل سهمیه، دکمه خرید برای همان کاربر حذف می‌شود، در حالی که سایر کاربران دارای سهمیه همچنان امکان خرید دارند.

## رزرو پنج‌دقیقه‌ای فاکتور

پس از ساخت فاکتور پیشنهاد، تعداد درخواستی به‌مدت پنج دقیقه برای همان کاربر رزرو می‌شود. فاکتور زمان پایان رزرو را نشان می‌دهد. اگر کاربر در این زمان از کیف پول پرداخت نکند، فاکتور به‌صورت خودکار منقضی و موجودی آزاد می‌شود. اگر کمتر از پنج دقیقه تا پایان پیشنهاد باقی مانده باشد، فاکتور همچنان صادر می‌شود اما موجودی رزرو نمی‌شود؛ هنگام پرداخت، فعال‌بودن طرح، سهمیه کاربر و موجودی فروش به‌صورت اتمیک دوباره بررسی می‌شوند.

## نوار پیشرفت فروش

صفحه پیشنهاد نوار پیشرفت ده‌قسمتی، تعداد خریدهای موفق، تعداد در حال رزرو و موجودی آماده خرید را جداگانه نمایش می‌دهد. نوار فقط با خرید موفق جلو می‌رود و ساخت فاکتور باعث افزایش فروش نمایش‌داده‌شده نمی‌شود.

پس از رسیدن موجودی به صفر یا پایان زمان، پیشنهاد بدون نیاز به کرون‌جاب از منوی اصلی حذف و امکان ایجاد فاکتور جدید برای آن بسته می‌شود.

## به‌روزرسانی دیتابیس

ساخت جدول‌ها و ستون‌های جدید هنگام اولین اجرای ربات به‌صورت خودکار انجام می‌شود و برای نصب‌های قبلی نیز سازگار است.
